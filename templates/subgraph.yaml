specVersion: 0.0.2
description: conquest.eth
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: OuterSpace
    network: {{chainName}}
    source:
      address: '{{contracts.OuterSpace.address}}'
      abi: OuterSpaceContract
      startBlock: {{contracts.OuterSpace.receipt.blockNumber}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.3
      language: wasm/assemblyscript
      entities:
        - Planet
      abis:
        - name: OuterSpaceContract
          file: ./abis/OuterSpace.json
      eventHandlers:
        - event: PlanetStake(indexed address,indexed uint256,uint32,uint256)
          handler: handlePlanetStake
        - event: FleetSent(indexed address,indexed uint256,uint256,uint32,uint32)
          handler: handleFleetSent
        - event: FleetArrived(indexed uint256,indexed address,indexed address,uint256,uint32,uint32,uint32,uint32,bool,uint32)
          handler: handleFleetArrived
        - event: PlanetExit(indexed address,indexed uint256)
          handler: handleExit
        - event: StakeToWithdraw(indexed address,uint256)
          handler: handleStakeToWithdraw
        - event: ExitComplete(indexed address,indexed uint256,uint256)
          handler: handleExitComplete
      file: ./src/mapping.ts
  - kind: ethereum/contract
    name: PlayToken_L2
    network: {{chainName}}
    source:
      address: '{{contracts.PlayToken_L2.address}}'
      abi: PlayToken_L2_Contract
      startBlock: {{contracts.PlayToken_L2.receipt.blockNumber}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.3
      language: wasm/assemblyscript
      entities:
        - Owner
      abis:
        - name: PlayToken_L2_Contract
          file: ./abis/PlayToken_L2.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handlePlayTokenTransfer
      file: ./src/mapping.ts
